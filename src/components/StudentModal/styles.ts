import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    modalTextInput: {
        marginTop: '10px'
    },
    modalButton: {
        marginTop: 15,
        marginBottom: 30,
        padding: '15px 0'
    }
})